language: ruby
cache: bundler

rvm:
  - 2.1
  - 2.2

env:
  - DB=postgresql

script:
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rake db:test:prepare
  - bundle exec rake

addons:
  postgresql: "9.3"
